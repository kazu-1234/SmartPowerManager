<Application
    x:Class="SmartPowerManager.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SmartPowerManager">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!-- BlueShift / Auto Dark Mode と同値のコンテンツ最大幅 -->
            <x:Double x:Key="PageContentMaxWidth">1000</x:Double>
            <x:Double x:Key="SettingsCardSpacing">4</x:Double>
            <x:Double x:Key="CompactNumberBoxWidth">52</x:Double>
            <!-- 時/分/月/日は同一幅（縦列揃え）。文字＋矢印が収まる最小寄り -->
            <x:Double x:Key="CompactComboBoxPartWidth">62</x:Double>
            <x:Double x:Key="CompactComboBoxYearWidth">78</x:Double>
            <x:Double x:Key="CompactComboBoxWeekdayWidth">74</x:Double>
            <x:Double x:Key="CompactComboBoxThemeWidth">82</x:Double>

            <Style x:Key="PageHostScrollViewerStyle" TargetType="ScrollViewer">
                <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
                <Setter Property="VerticalScrollBarVisibility" Value="Disabled" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="VerticalAlignment" Value="Stretch" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Top" />
            </Style>

            <!-- 各ページ共通（BlueShift の PageBaseStackPanelStyle と同じ） -->
            <Style x:Key="PageBaseStackPanelStyle" TargetType="StackPanel">
                <Setter Property="MaxWidth" Value="{StaticResource PageContentMaxWidth}" />
                <Setter Property="Margin" Value="24,0,24,24" />
                <Setter Property="Spacing" Value="{StaticResource SettingsCardSpacing}" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
            </Style>

            <!-- スケジュール系 Grid: 左右・下は BlueShift / 情報・設定と同じ -->
            <Style x:Key="PageBaseContentGridStyle" TargetType="Grid">
                <Setter Property="MaxWidth" Value="{StaticResource PageContentMaxWidth}" />
                <Setter Property="Margin" Value="24,0,24,24" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
            </Style>

            <Style x:Key="SchedulePageStackPanelStyle" TargetType="StackPanel">
                <Setter Property="Margin" Value="0" />
                <Setter Property="Spacing" Value="{StaticResource SettingsCardSpacing}" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
            </Style>

            <Style x:Key="CardFormTitleStyle" TargetType="TextBlock" BasedOn="{StaticResource CardHeaderTextStyle}">
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

            <!-- 情報・設定・機能ページ共通のページタイトル位置 -->
            <Style x:Key="PageTitleTextStyle" TargetType="TextBlock" BasedOn="{StaticResource TitleTextBlockStyle}">
                <Setter Property="Margin" Value="0,20,0,12" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
            </Style>

            <Style x:Key="CardHeaderTextStyle" TargetType="TextBlock" BasedOn="{StaticResource BodyStrongTextBlockStyle}">
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
            </Style>

            <Style x:Key="CardDescriptionTextStyle" TargetType="TextBlock" BasedOn="{StaticResource CaptionTextBlockStyle}">
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

            <Style x:Key="SectionHeaderStyle" TargetType="TextBlock" BasedOn="{StaticResource BodyStrongTextBlockStyle}">
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
                <Setter Property="Margin" Value="0,0,0,8" />
            </Style>

            <Style x:Key="SettingsSectionHeaderStyle" TargetType="TextBlock" BasedOn="{StaticResource SectionHeaderStyle}">
                <Setter Property="Margin" Value="0,28,0,4" />
            </Style>

            <Style x:Key="SettingsSectionHeaderFirstStyle" TargetType="TextBlock" BasedOn="{StaticResource SettingsSectionHeaderStyle}">
                <Setter Property="Margin" Value="0,8,0,4" />
            </Style>

            <Style x:Key="PrimaryActionButtonStyle" TargetType="Button" BasedOn="{StaticResource AccentButtonStyle}">
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="HorizontalAlignment" Value="Left" />
            </Style>

            <Style x:Key="SecondaryActionButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="Padding" Value="14,8" />
                <Setter Property="HorizontalAlignment" Value="Left" />
            </Style>

            <Style x:Key="QuickActionButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="MinWidth" Value="44" />
                <Setter Property="Padding" Value="12,8" />
            </Style>

            <Style x:Key="PatternListItemContainerStyle" TargetType="ListViewItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Margin" Value="0,0,0,8" />
                <Setter Property="MinHeight" Value="56" />
                <Setter Property="MinWidth" Value="0" />
            </Style>

            <Style x:Key="DeleteIconButtonStyle" TargetType="Button" BasedOn="{StaticResource SubtleButtonStyle}">
                <Setter Property="Width" Value="36" />
                <Setter Property="Height" Value="36" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>

            <Style x:Key="ScheduleToggleSwitchStyle" TargetType="ToggleSwitch">
                <!-- レイアウト幅をスイッチ本体分確保し、右端で切れないようにする -->
                <Setter Property="MinWidth" Value="44" />
                <Setter Property="MaxWidth" Value="44" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Margin" Value="4,0,8,0" />
                <Setter Property="HorizontalAlignment" Value="Right" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="OnContent" Value="{x:Null}" />
                <Setter Property="OffContent" Value="{x:Null}" />
            </Style>

            <Style x:Key="FormRowStackPanelStyle" TargetType="StackPanel">
                <Setter Property="Orientation" Value="Horizontal" />
                <Setter Property="Spacing" Value="6" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="HorizontalAlignment" Value="Right" />
                <Setter Property="Padding" Value="0,0,2,0" />
            </Style>

            <!-- 既定 ComboBoxPadding(12,5,0,7) より詰める。矢印列は CompactComboBoxHelper で調整 -->
            <Thickness x:Key="ComboBoxPadding">6,2,0,2</Thickness>

            <Style x:Key="CompactComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource DefaultComboBoxStyle}">
                <Setter Property="Padding" Value="6,2,0,2" />
                <Setter Property="Width" Value="{StaticResource CompactComboBoxPartWidth}" />
                <Setter Property="MinWidth" Value="{StaticResource CompactComboBoxPartWidth}" />
                <Setter Property="MaxWidth" Value="{StaticResource CompactComboBoxPartWidth}" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
            </Style>

            <Style x:Key="CompactComboBoxYearStyle" TargetType="ComboBox" BasedOn="{StaticResource CompactComboBoxStyle}">
                <Setter Property="Width" Value="{StaticResource CompactComboBoxYearWidth}" />
                <Setter Property="MinWidth" Value="{StaticResource CompactComboBoxYearWidth}" />
                <Setter Property="MaxWidth" Value="{StaticResource CompactComboBoxYearWidth}" />
            </Style>

            <Style x:Key="CompactComboBoxMonthDayStyle" TargetType="ComboBox" BasedOn="{StaticResource CompactComboBoxStyle}">
                <Setter Property="Width" Value="{StaticResource CompactComboBoxPartWidth}" />
                <Setter Property="MinWidth" Value="{StaticResource CompactComboBoxPartWidth}" />
                <Setter Property="MaxWidth" Value="{StaticResource CompactComboBoxPartWidth}" />
            </Style>

            <Style x:Key="CompactComboBoxWeekdayStyle" TargetType="ComboBox" BasedOn="{StaticResource CompactComboBoxStyle}">
                <Setter Property="Width" Value="{StaticResource CompactComboBoxWeekdayWidth}" />
                <Setter Property="MinWidth" Value="{StaticResource CompactComboBoxWeekdayWidth}" />
                <Setter Property="MaxWidth" Value="{StaticResource CompactComboBoxWeekdayWidth}" />
            </Style>

            <Style x:Key="CompactComboBoxThemeStyle" TargetType="ComboBox" BasedOn="{StaticResource CompactComboBoxStyle}">
                <Setter Property="Width" Value="{StaticResource CompactComboBoxThemeWidth}" />
                <Setter Property="MinWidth" Value="{StaticResource CompactComboBoxThemeWidth}" />
                <Setter Property="MaxWidth" Value="{StaticResource CompactComboBoxThemeWidth}" />
            </Style>

            <Style x:Key="CompactNumberBoxStyle" TargetType="NumberBox">
                <Setter Property="Width" Value="{StaticResource CompactNumberBoxWidth}" />
                <Setter Property="Padding" Value="4,2" />
                <Setter Property="SpinButtonPlacementMode" Value="Inline" />
                <Setter Property="SmallChange" Value="1" />
                <Setter Property="HorizontalAlignment" Value="Left" />
            </Style>

            <Style x:Key="ScheduleFormRowStyle" TargetType="Grid">
                <Setter Property="Padding" Value="0,4" />
                <Setter Property="VerticalAlignment" Value="Stretch" />
            </Style>

            <Style x:Key="ScheduleCardBorderStyle" TargetType="Border">
                <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
                <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="Padding" Value="12,10,18,10" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>

            <Style x:Key="ScheduleRowDividerStyle" TargetType="Border">
                <Setter Property="Height" Value="1" />
                <Setter Property="Background" Value="{ThemeResource DividerStrokeColorDefaultBrush}" />
                <Setter Property="Opacity" Value="0.5" />
                <Setter Property="VerticalAlignment" Value="Bottom" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
