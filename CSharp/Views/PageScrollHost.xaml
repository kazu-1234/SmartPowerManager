<ContentControl
    x:Class="SmartPowerManager.Views.PageScrollHost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:SmartPowerManager.Views"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    HorizontalContentAlignment="Stretch"
    VerticalContentAlignment="Stretch">
    <ContentControl.Template>
        <ControlTemplate TargetType="views:PageScrollHost">
            <ScrollViewer x:Name="PART_ScrollViewer"
                          Style="{StaticResource PageHostScrollViewerStyle}">
                <!-- Grid でビューポート幅を確定させ、子の SettingsCard 横幅を揃える -->
                <Grid x:Name="PART_ContentHost"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Top">
                    <ContentPresenter x:Name="PART_ContentPresenter"
                                      HorizontalAlignment="Stretch"
                                      VerticalAlignment="Top"
                                      HorizontalContentAlignment="Stretch"
                                      Content="{TemplateBinding Content}"
                                      ContentTemplate="{TemplateBinding ContentTemplate}" />
                </Grid>
            </ScrollViewer>
        </ControlTemplate>
    </ContentControl.Template>
</ContentControl>
