<Page
    x:Class="SmartPowerManager.Views.InfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:views="using:SmartPowerManager.Views">
    <views:PageScrollHost>
        <StackPanel Style="{StaticResource PageBaseStackPanelStyle}">
            <TextBlock Text="情報" Style="{StaticResource PageTitleTextStyle}" />

            <controls:SettingsCard>
                <controls:SettingsCard.Header>
                    <TextBlock Text="アプリについて" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                </controls:SettingsCard.Header>
                <StackPanel Spacing="8">
                    <TextBlock x:Name="VersionText" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                    <TextBlock Text="PC のシャットダウン・再起動スケジュール管理と Pico W / GAS 経由の WoL 遠隔起動を行います。"
                               Style="{StaticResource BodyTextBlockStyle}"
                               TextWrapping="Wrap" />
                </StackPanel>
            </controls:SettingsCard>

            <controls:SettingsCard>
                <controls:SettingsCard.Header>
                    <TextBlock Text="免責事項" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                </controls:SettingsCard.Header>
                <controls:SettingsCard.Description>
                    <TextBlock Text="本ソフトウェアの使用により生じたいかなる損害についても、開発者は一切の責任を負いません。自己責任でご使用ください。"
                               Style="{StaticResource BodyTextBlockStyle}"
                               TextWrapping="Wrap" />
                </controls:SettingsCard.Description>
            </controls:SettingsCard>
        </StackPanel>
    </views:PageScrollHost>
</Page>
