<Page
    x:Class="SmartPowerManager.Views.UpdatePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:views="using:SmartPowerManager.Views">
    <views:PageScrollHost>
        <StackPanel Style="{StaticResource PageBaseStackPanelStyle}">
            <TextBlock Text="アップデート" Style="{StaticResource PageTitleTextStyle}" />

            <controls:SettingsCard>
                <controls:SettingsCard.Header>
                    <TextBlock x:Name="VersionText" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                </controls:SettingsCard.Header>
                <Button x:Name="CheckUpdateButton"
                        Content="アップデートを確認"
                        Style="{StaticResource PrimaryActionButtonStyle}"
                        Click="CheckUpdateButton_Click" />
            </controls:SettingsCard>

            <InfoBar x:Name="UpdateInfoBar" IsOpen="False" IsClosable="True" />

            <controls:SettingsCard x:Name="InstallUpdateCard" Visibility="Collapsed">
                <controls:SettingsCard.Description>
                    <StackPanel Spacing="8">
                        <TextBlock x:Name="InstallStatusText" TextWrapping="Wrap" />
                        <Button x:Name="InstallUpdateButton"
                                Content="ダウンロードしてインストール"
                                Style="{StaticResource PrimaryActionButtonStyle}"
                                Click="InstallUpdateButton_Click" />
                    </StackPanel>
                </controls:SettingsCard.Description>
            </controls:SettingsCard>

            <controls:SettingsCard>
                <controls:SettingsCard.Header>
                    <TextBlock Text="免責事項" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                </controls:SettingsCard.Header>
                <controls:SettingsCard.Description>
                    <TextBlock Text="本ソフトウェアの使用により生じたいかなる損害についても、開発者は一切の責任を負いません。自己責任でご使用ください。"
                               Style="{StaticResource BodyTextBlockStyle}"
                               TextWrapping="Wrap" />
                </controls:SettingsCard.Description>
            </controls:SettingsCard>
        </StackPanel>
    </views:PageScrollHost>
</Page>
