# SmartPowerManager

<img width="902" height="689" alt="image" src="https://github.com/user-attachments/assets/fd9ff105-f864-4f9e-a028-9bc6da35100c" />

## 開発の背景と目的
夜中にPCで作業や動画視聴を行っている最中に寝落ちしてしまい、朝まで無駄にPCが稼働し続けてしまう「切り忘れ問題」は、電力の無駄遣いだけでなくPCパーツの寿命にも影響します。
また、自宅で稼働させているサーバー用PCにおいては、システムの動作を安定させるために定期的な再起動が必要ですが、これを手動で毎回行うのは手間がかかります。

本ソフトウェアは、これらの課題を解決し、ユーザーが意識することなくPCのシャットダウンや再起動を安全かつ自動的に実行・管理するために開発されました。

## システム概要
Windows PCの電源操作（シャットダウン・再起動）を柔軟にスケジュール管理する常駐型アプリケーションです。
さらに、Raspberry Pi Pico Wと連携することで、ローカルネットワーク外からWake on LAN (WoL) を用いたPCの遠隔起動を行う統合的な電源管理システムとして機能します。

## 主な機能

### PC電源スケジュール管理
* **シャットダウンと再起動の完全分離**: タブで処理を分け、同時刻に設定した際の競合を防ぐ排他制御を実装しています。
* **クイックタイマー設定**: 現在時刻から「1時間後」「3時間後」など、数クリックでスケジュールを追加できます（寝落ち対策に最適です）。
* **柔軟なスケジュール設定**: 毎日、毎週（曜日指定）、一回限り（指定日時）の3種類からスケジュールを設定可能です。

### バックグラウンド動作の最適化
* **タスクトレイ常駐**: アプリケーションウィンドウを閉じても、タスクトレイに常駐してバックグラウンドで監視を継続します。
* **スタートアップ登録**: Windows起動時に自動で実行されるよう設定可能です。
* **安全機能**: スケジュール実行の60秒前にカウントダウンダイアログを表示します。意図しないタイミングでのシャットダウンを防ぐため、この画面から実行のキャンセルが可能です。

### Raspberry Pi Pico W 連携による遠隔操作
* **スケジュールの自動同期**: PCアプリから設定した起動スケジュールを、自動的にPico W側へ送信し保存させます。
* **Web UI管理**: Pico WのIPアドレスにブラウザからアクセスし、スマートフォンなどから直接スケジュールを変更したり、即座にWoLマジックパケットを送信することができます。
* **LINE・外部からのトリガー受信**: Google Apps Script (GAS) への定期的なポーリングにより、LINEなどの外部サービスからの起動コマンドを受信して対象PCを起動させます。

### 自動アップデート
* GitHubから最新のリリース情報を取得し、アプリ単体でアップデートを完結させます。実行中のファイルを安全に置き換えるRename-Swap方式を採用しているため、セキュリティソフトの誤検知やアクセス拒否エラーを防ぎます。

## 動作環境

* **PC側**: Windows 10 / 11
* **マイコン側**: Raspberry Pi Pico W (WoLや外部連携を使用する場合)
  * 開発環境: Arduino IDE

## 免責事項
本ソフトウェアの使用により生じたいかなる損害（作業中の未保存データの消失、システム不具合など）についても、開発者は一切の責任を負いません。自己責任でご使用ください。

## ライセンス
MIT LICENSE
